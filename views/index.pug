extends layout

block content
  h1= title
  p Hello world #{title}
  
      form(action='test', method="get")
       input(type='text', name='search', placeholder='Search Database')
       input(type='submit')
       